import { GridConfig } from '../engine/grid';

export const DEFAULT_GRID_CONFIG: Readonly<GridConfig>  = {
  color: '#000000',
  isEnable: false,
  width: 1,
  size: 8,
  minZoom: 0.5,
  opacity: 1,
}
